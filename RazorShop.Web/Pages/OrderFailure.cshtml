@inherits RazorSlice<OrderFailureVm>
@implements IUsesLayout<_CheckoutLayout, LayoutModel>

@(await RenderPartialAsync<OrderFailure, OrderFailureVm>(Model))

@functions {
    public LayoutModel LayoutModel => new() {
            Title = "Order Failed",
        };
}