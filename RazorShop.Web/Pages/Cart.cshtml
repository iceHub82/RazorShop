@inherits RazorSliceHttpResult<CheckoutCartVm>
@implements IUsesLayout<_Layout, LayoutModel>

@(await RenderPartialAsync<Cart, CheckoutCartVm>(Model))

@functions {
    public LayoutModel LayoutModel => new() {
            Title = "Cart Checkout",
        };
}