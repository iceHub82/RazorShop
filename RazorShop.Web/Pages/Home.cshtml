@inherits RazorSlice<ProductsVm>
@implements IUsesLayout<_Layout, LayoutModel>

@(await RenderPartialAsync<Products, ProductsVm>(Model))

@functions {
    public LayoutModel LayoutModel => new() {
        Title = "Artform.dk – Generativ kunst og T-shirts",
        Description = "Køb originale plottede tegninger og kunst-T-shirts. Generativ kunst og unikke motiver fra Artform.dk – håndlavet i Danmark.",
        JsonLd = Model.JsonLd,
        HasHeader = true
    };
}