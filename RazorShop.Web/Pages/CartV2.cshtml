@inherits RazorSliceHttpResult<CheckoutCartVm>
@implements IUsesLayout<_CheckoutLayout, LayoutModel>

@(await RenderPartialAsync<CartV2, CheckoutCartVm>(Model))

@functions {
    public LayoutModel LayoutModel => new() {
            Title = "Cart",
        };
}